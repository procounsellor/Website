export async function sendOtp(phone: string) {
    console.log(phone)
    // waiting for sendotp api because MGG doesn't allow calling from browser
}

export async function verifyOtp(phone: string, otp: string) {
   console.log(phone, otp)

//    same reason
}